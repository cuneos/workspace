# Use the new v6 lockfile format, which will be the default one in pnpm v8.
use-lockfile-v6=true

# If this is enabled, commands will fail if there is a missing or invalid peer dependency in the tree.
strict-peer-dependencies=false

# When true, any missing non-optional peer dependencies are automatically installed.
auto-install-peers=true

# If you know that only some flawed packages have phantom dependencies, 
# you can use this option to exclusively hoist the phantom dependencies (recommended).
hoist-pattern[]=*eslint*